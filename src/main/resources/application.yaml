spring:
  datasource:
    url: jdbc:mysql://localhost:46006/demo
    username: root
    password: my-secret-pw
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect

